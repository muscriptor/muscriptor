name: Deploy demo

on:
  workflow_dispatch:

concurrency:
  group: deploy-demo
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up SSH access to the deployment host
      run: |
        mkdir -p ~/.ssh
        chmod 700 ~/.ssh
        echo "${{ secrets.DEPLOYMENT_SSH_KEY }}" > ~/.ssh/id_deploy
        chmod 600 ~/.ssh/id_deploy
        # docker -H ssh://... uses the system ssh client, which reads this config.
        printf 'Host muscriptor.kyutai.org\n  IdentityFile ~/.ssh/id_deploy\n  IdentitiesOnly yes\n' > ~/.ssh/config
        # Trust the host key so the ssh connection isn't blocked by the prompt.
        ssh-keyscan muscriptor.kyutai.org >> ~/.ssh/known_hosts
    - name: Deploy
      env:
        HF_TOKEN: ${{ secrets.DEPLOYMENT_HF_TOKEN }}
      run: ./deploy.sh
